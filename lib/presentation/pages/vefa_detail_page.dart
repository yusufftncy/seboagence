import 'package:flutter/material.dart';

class VefaDetailPage extends StatelessWidget {
  const VefaDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vefa')),
      body: const Center(
        child: Text('Vefa sayfası yakında.'),
      ),
    );
  }
}
